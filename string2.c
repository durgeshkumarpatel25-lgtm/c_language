//Example of gets and puts gets means me input from the user and puts means print msg
#include<stdio.h>
#include<conio.h>
void main()
{
    char fullName[100];
    gets(fullName);
    puts(fullName);
    getch();
}