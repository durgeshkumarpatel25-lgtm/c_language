#include <stdio.h>
#include <conio.h>
void main()
{
    float radius;
    printf("Enter the radius: ");
    scanf("%f",&radius);
    printf("\n The radius is: %f",3.14 * radius * radius);
    getch();
}
